/** Session cookie name. */
export const SESSION_COOKIE_NAME = 'slop_session';

/** Session max age in milliseconds (24 hours). */
export const SESSION_MAX_AGE_MS = 86_400_000;

/** OAuth state random bytes length. */
export const OAUTH_STATE_BYTES = 16;
