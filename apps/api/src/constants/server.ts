/** Default HTTP port for the API server. */
export const DEFAULT_API_PORT = 3001;

/** Minimum length for session signing secret. */
export const MIN_SESSION_SECRET_LENGTH = 32;

/** Liveness probe path. */
export const HEALTH_ROUTE = '/health';
