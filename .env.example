# Copy to .env for docker-compose / make deploy-local (never commit .env)

# Backend
GITHUB_TOKEN=
ALLOWED_ORIGINS=http://localhost:3000

# Frontend (docker-compose.prod build arg)
NEXT_PUBLIC_API_URL=http://localhost:8000
