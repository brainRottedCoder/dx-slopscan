import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Score Simulator (Educational) | DX SlopScan',
}

export default function SimulatorLayout({ children }: { children: React.ReactNode }) {
  return children
}
